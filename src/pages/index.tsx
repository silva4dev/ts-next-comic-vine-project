import React from "react";

import { DefaultLayout as Layout } from "@/layouts/default";

export default function Index() {
    return (
        <Layout>
            <h1 className="text-blue-500 text-2xl">Home works!</h1>
        </Layout>
    );
}
